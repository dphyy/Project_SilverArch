# `scripts/`

Utility scripts for local development and QA.

- `report-qa.mjs` is used for report rendering checks.

Scripts should be safe for local/demo data and should not require production secrets.

